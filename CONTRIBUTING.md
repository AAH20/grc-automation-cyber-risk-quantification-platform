# Contributing

Contributions should include tests and preserve the evidence boundary. Framework mappings require explicit direction, rationale, coverage, source attribution where available, and reviewer information. Do not submit proprietary framework text or real organizational evidence without authorization.

Run:

```bash
python3 -m unittest discover -s tests -v
npm run build
```
